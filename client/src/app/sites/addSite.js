(function(module) {

    module.controller('AddSiteController', function () {
        var model = this;
        model.site = {};
        model.saveSite = saveSite;

        init();

        function init() {

        }

        function saveSite() {

        }
    });

}(angular.module("kazuku.sites")));
