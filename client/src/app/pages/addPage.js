(function(module) {

    module.controller('AddPageController', function () {
        var model = this;
        model.page = {};
        model.savePage = savePage;

        init();

        function init() {

        }

        function savePage() {

        }
    });

}(angular.module("kazuku.pages")));
