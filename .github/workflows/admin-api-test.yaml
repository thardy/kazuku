name: admin-api-test

on:
  pull_request:
    paths:
      - 'admin-api/**'

jobs:
  test:
    runs-on: ubuntu-latest
    environment: dev
    env:
      KAZUKU_ENV: ${{ env.KAZUKU_ENV }}
    steps:
      - name: Output Environment Variables
        run: |
          echo "KAZUKU_ENV: $KAZUKU_ENV"
      - uses: actions/checkout@v4
      - run: cd admin-api && npm install && npm run test:ci
