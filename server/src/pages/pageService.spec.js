var should = require("should");
var pageService = require("./pageService");

describe("Page Service", function () {
    before(function () {

    });
    after(function () {

    });

    it("can get all Pages");

    it("can get a Page by id");

    it("can create a Page");

    it("can update a Page");
});
