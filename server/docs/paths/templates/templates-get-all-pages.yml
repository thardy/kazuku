get:
  tags:
      - templates
  summary: Get all page templates
  description: Get all page templates for this org
  operationId: getPageTemplates
  responses:
    '200':
      description: successful operation
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../schemas/template.yml'